# Copy to .env (gitignored) for local scripts. Lambda reads these from SSM Parameter Store instead.
OPENAI_API_KEY=
GITHUB_TOKEN=
# Comma-separated owner/repo list, e.g. kairo-labs/app,kairo-labs/api
GITHUB_REPOS=
BRIEFING_EMAIL=
TASKS_TABLE=orbit-tasks
REPORTS_BUCKET=
